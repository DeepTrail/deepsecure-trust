# pytest.ini
[pytest]
testpaths = tests
markers =
    unit: Unit tests (run by default)
    integration: Integration tests (require backend services)
    e2e: End-to-end tests (require full stack with all services running)
    critical: Critical tests that must pass for release
    slow: Slow tests to skip in quick runs
    requires_gateway_auth: Tests requiring authenticated gateway setup
    live_gcp: Tests requiring a real GCP environment (Cloud Run / GCE / GKE) 