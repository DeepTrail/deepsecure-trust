#!/usr/bin/env bash
# Merge Point Configuration: afk-workflow-enablement MP2 (Skills Complete)
# Auto-generated by /run-plan on 2026-06-17
# Used by: scripts/execute_merge_point.sh

MP_ID="MP2"
WORKSTREAM="afk-workflow-enablement"
WORKSTREAM_DIR="docs/workstreams/afk-workflow-enablement"

# Single-branch mode
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="MP2: AFK skills complete — 10 new skills"
MERGE_MSG=""

SYNC_PREFIX=""

# Test commands
UNIT_TESTS=(
    "for s in afk babysit-pr autofix-pr security-scan grill-me verify-app context-engineering triage afk-summary go; do test -f .claude/commands/\${s}.md && test -s .claude/commands/\${s}.md || exit 1; done"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment (for /verify-app skill testing)
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control deeptrail-gateway"
RESTART_CMD="docker compose up -d deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE=""
LOGIN_EMAIL=""
LOGIN_PASSWORD=""

SMOKE_ENDPOINTS=()

# Smoke tests — all 10 skills exist and are non-empty
SMOKE_TESTS=(
    "test -s .claude/commands/afk.md"
    "test -s .claude/commands/babysit-pr.md"
    "test -s .claude/commands/autofix-pr.md"
    "test -s .claude/commands/security-scan.md"
    "test -s .claude/commands/grill-me.md"
    "test -s .claude/commands/verify-app.md"
    "test -s .claude/commands/context-engineering.md"
    "test -s .claude/commands/triage.md"
    "test -s .claude/commands/afk-summary.md"
    "test -s .claude/commands/go.md"
)

COMMIT_PATTERN="skills\|afk\|MP2"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

# Custom success criteria
SUCCESS_CRITERIA=(
    "ls .claude/commands/*.md | wc -l | grep -q '[3-9][0-9]'"
    "for s in afk babysit-pr autofix-pr security-scan grill-me verify-app context-engineering triage afk-summary go; do test -s .claude/commands/\${s}.md || exit 1; done"
)
