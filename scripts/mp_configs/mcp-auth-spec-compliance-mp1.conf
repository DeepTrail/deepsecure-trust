#!/usr/bin/env bash
# Merge Point Configuration: mcp-auth-spec-compliance MP1 (Stateless Gateway Complete)
# Auto-generated by /run-plan on 2026-05-29
# Used by: scripts/execute_merge_point.sh

MP_ID="MP1"
WORKSTREAM="mcp-auth-spec-compliance"
WORKSTREAM_DIR="docs/workstreams/mcp-auth-spec-compliance"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="MP1: Stateless Gateway complete — MCP 2026-07-28 protocol support"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deeptrail-gateway && pytest tests/mcp/ -v"
    "cd deeptrail-gateway && pytest tests/middleware/ -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-gateway"
RESTART_CMD="docker compose up -d deeptrail-gateway"
HEALTH_URL="http://localhost:8002/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8002"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Gateway health check"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deeptrail-gateway/app/mcp/handlers/discover.py"
    "test -f deeptrail-gateway/tests/mcp/test_stateless_tools.py"
    "test -f deeptrail-gateway/tests/mcp/test_discover.py"
    "test -f deeptrail-gateway/tests/mcp/test_header_validation.py"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="stateless\|mcp-auth-spec-compliance"

# Alembic migration revision (empty if no migration)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command (empty if not needed)
DB_CHECK_CMD=""

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "cd deeptrail-gateway && pytest tests/mcp/ -v --tb=short"
    "cd deeptrail-gateway && pytest tests/middleware/ -v --tb=short"
)
