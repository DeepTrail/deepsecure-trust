#!/usr/bin/env bash
# Merge Point Configuration: p5.2-gap-closure MP4 (IdP Admin Complete)
# Auto-generated by /run-plan on 2026-06-06

MP_ID="MP4"
WORKSTREAM="p5.2-gap-closure"
WORKSTREAM_DIR="docs/workstreams/p5.2-gap-closure"

WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P3-B1: IdP admin complete — DB group→role mappings + CRUD UI"
MERGE_MSG=""
SYNC_PREFIX=""

UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/api/test_admin_idp.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

MIGRATION_CMD="docker compose exec deeptrail-control alembic upgrade head"
BUILD_CMD="docker compose build deeptrail-control frontend"
RESTART_CMD="docker compose up -d db redis deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
)

SMOKE_TESTS=(
    "test -f deeptrail-control/app/api/v1/endpoints/admin_idp.py"
    "test -f frontend/src/app/(dashboard)/dashboard/admin/idp/page.tsx"
)

COMMIT_PATTERN="idp\|group.role\|p5.2-gap-closure"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD="docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT count(*) FROM information_schema.tables WHERE table_name='idp_group_role_mappings'\" -t"

SUCCESS_CRITERIA=(
    "grep -q idp_group_role_mappings deeptrail-control/app/models/idp_group_role_mapping.py || test -f deeptrail-control/app/models/idp_group_role_mapping.py"
)
