#!/usr/bin/env bash
# Merge Point Configuration: p5.4-sdk-integration-guides MP2 (Phase 2+3 Proxy + SDK Restructure)
# Auto-generated by /run-plan on June 8, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP2"
WORKSTREAM="p5.4-sdk-integration-guides"
WORKSTREAM_DIR="docs/workstreams/p5.4-sdk-integration-guides"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P5.4 Phase 2+3: Stdio Proxy + SDK Restructure"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deepsecure-proxy && pytest tests/ -v --tb=short"
    "pytest tests/test_package_split.py -v --tb=short"
    "pytest tests/_core/ -v --tb=short"
)
INTEGRATION_TESTS=(
    "cd deepsecure-proxy && pytest tests/test_proxy_e2e.py -v -m e2e --tb=short"
)
REGRESSION_TESTS=(
    "make test"
    "make lint"
)
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD=""
RESTART_CMD="docker compose up -d db redis deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Control Plane Health"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deepsecure-proxy/pyproject.toml"
    "test -f deepsecure-proxy/deepsecure_proxy/server.py"
    "test -f deepsecure-proxy/deepsecure_proxy/jwt_manager.py"
    "test -f deepsecure-proxy/deepsecure_proxy/delegation_rotator.py"
    "test ! -f deepsecure/_core/client.py"
    "test ! -f deepsecure/resources/agents.py"
    "test ! -f deepsecure/vault.py"
    "test ! -f deepsecure/policy.py"
    "python -c 'from deepsecure import bootstrap, Client'"
    "python -c 'import httpx'"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="proxy\|Phase 2\|Phase 3\|restructure\|p5.4"

# Alembic migration revision (empty if no migration)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command (empty if not needed)
DB_CHECK_CMD=""

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "deepsecure-proxy --help"
    "python -c 'from deepsecure import bootstrap, Client; print(\"ok\")'"
    "cd deepsecure-proxy && pytest tests/ -v --tb=short"
    "pytest tests/test_package_split.py -v --tb=short"
    "make test"
    "make lint"
)
