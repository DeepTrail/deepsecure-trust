#!/usr/bin/env bash
# Merge Point Configuration: it-admin-service-catalog-mcp-mgmt MP2 (Backend APIs Complete)
# Auto-generated by /run-plan on May 29, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP2"
WORKSTREAM="it-admin-service-catalog-mcp-mgmt"
WORKSTREAM_DIR="docs/workstreams/it-admin-service-catalog-mcp-mgmt"

# Git worktree / branch configuration
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P0-B2: Backend APIs complete — service catalog, fleet, multi-user runtime, emergency"
MERGE_MSG=""
SYNC_PREFIX=""

# Test commands
UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/services/test_service_registry_service.py -v"
    "cd deeptrail-control && pytest tests/api/test_admin_services.py -v"
    "cd deeptrail-control && pytest tests/api/test_admin_fleet.py -v"
    "cd deeptrail-control && pytest tests/api/test_admin_emergency.py -v"
    "cd deeptrail-control && pytest tests/services/test_delegation_template_service.py -v"
    "cd deeptrail-gateway && pytest tests/handlers/test_tools_call_multiuser.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control deeptrail-gateway"
RESTART_CMD="docker compose up -d deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Control health"
    "GET|/api/v1/admin/services|.services|List services (admin)"
    "GET|/api/v1/admin/agents|.agents|List agents (admin)"
    "GET|/api/v1/admin/delegation-templates|.templates|List templates (admin)"
    "GET|/api/v1/internal/services/registry|.services|Internal registry"
)

# Non-API smoke tests
SMOKE_TESTS=(
    "test -f deeptrail-control/app/services/service_registry_service.py"
    "test -f deeptrail-control/app/api/v1/endpoints/admin_services.py"
    "test -f deeptrail-control/app/api/v1/endpoints/admin_fleet.py"
    "test -f deeptrail-control/app/api/v1/endpoints/admin_emergency.py"
    "test -f deeptrail-control/app/services/delegation_template_service.py"
    "test -f deeptrail-gateway/app/mcp/handlers/tools_call.py"
)

COMMIT_PATTERN="backend\|service_catalog\|fleet\|multi-user\|emergency"
EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

SUCCESS_CRITERIA=(
    "cd deeptrail-control && python -c 'from app.services.service_registry_service import ServiceRegistryService; print(\"OK\")'"
    "cd deeptrail-control && python -c 'from app.services.delegation_template_service import DelegationTemplateService; print(\"OK\")'"
)
