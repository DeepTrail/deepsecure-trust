#!/usr/bin/env bash
# Merge Point Configuration: p5.2-gap-closure MP1 (Foundation Complete)
# Auto-generated by /run-plan on 2026-06-06
# Used by: scripts/execute_merge_point.sh

MP_ID="MP1"
WORKSTREAM="p5.2-gap-closure"
WORKSTREAM_DIR="docs/workstreams/p5.2-gap-closure"

WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P0-B1: Foundation complete — role visibility + fleet filters"
MERGE_MSG=""
SYNC_PREFIX=""

UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/services/test_role_resolver.py tests/services/test_available_to.py -v"
    "cd deeptrail-control && pytest tests/api/test_services_catalog.py -v -k role"
    "cd deeptrail-control && pytest tests/api/test_admin_fleet.py -v -k \"filter or summary\""
    "cd deeptrail-control && pytest tests/api/test_admin_fleet.py -k identity_stack -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control frontend"
RESTART_CMD="docker compose up -d db redis deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
)

SMOKE_TESTS=(
    "test -f deeptrail-control/app/services/role_resolver.py"
    "test -f deeptrail-control/app/services/available_to.py"
)

COMMIT_PATTERN="foundation\|role_resolver\|fleet.filter\|p5.2-gap-closure"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

SUCCESS_CRITERIA=(
    "grep -q RoleResolver deeptrail-control/app/services/role_resolver.py"
    "grep -q lifecycle_state deeptrail-control/app/api/v1/endpoints/admin_fleet.py"
)
