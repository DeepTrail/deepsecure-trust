#!/usr/bin/env bash
# Merge Point Configuration: afk-workflow-enablement MP3 (CLAUDE.md + Hermes)
# Auto-generated by /run-plan on 2026-06-17
# Used by: scripts/execute_merge_point.sh

MP_ID="MP3"
WORKSTREAM="afk-workflow-enablement"
WORKSTREAM_DIR="docs/workstreams/afk-workflow-enablement"

# Single-branch mode
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="MP3: CLAUDE.md refactored + Hermes Phase 1-2 integrated"
MERGE_MSG=""

SYNC_PREFIX=""

# Test commands
UNIT_TESTS=(
    "test $(wc -l < CLAUDE.md) -le 200"
    "test -f docs/DEVELOPMENT_COMMANDS.md"
    "test -f docs/ARCHITECTURE.md"
    "test -f docs/TOKEN_TYPES.md"
    "test -f docs/TESTING_GUIDE.md"
    "test -f CODE_STANDARDS.md"
    "test -f .claude/compact-recovery.md"
    "test -f .hermes/config.yaml"
)
INTEGRATION_TESTS=(
    "python -m pytest tests/hermes/test_observer.py -v --tb=short"
    "python -m pytest tests/hermes/test_invoker.py -v --tb=short"
)
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD=""
RESTART_CMD=""
HEALTH_URL=""
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE=""
LOGIN_EMAIL=""
LOGIN_PASSWORD=""

SMOKE_ENDPOINTS=()

# Smoke tests
SMOKE_TESTS=(
    "test $(wc -l < CLAUDE.md) -le 200"
    "test -f docs/DEVELOPMENT_COMMANDS.md"
    "test -f docs/ARCHITECTURE.md"
    "test -f docs/TOKEN_TYPES.md"
    "test -f docs/TESTING_GUIDE.md"
    "test -f CODE_STANDARDS.md"
    "test -f .claude/compact-recovery.md"
    "test -f .hermes/config.yaml"
    "test -f .hermes/skills/afk-schedule.yaml"
    "test -f .hermes/skills/cross-session-review.yaml"
    "test -f scripts/hermes-setup.sh"
    "test -f scripts/hermes-observer.sh"
    "test -f scripts/hermes-invoker.sh"
    "test -f tests/hermes/test_observer.py"
    "test -f tests/hermes/test_invoker.py"
)

COMMIT_PATTERN="CLAUDE.md\|hermes\|MP3\|refactor"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

SUCCESS_CRITERIA=(
    "test $(wc -l < CLAUDE.md) -le 200"
    "grep -q 'token' .claude/compact-recovery.md"
    "python -m pytest tests/hermes/ -v --tb=short"
)
