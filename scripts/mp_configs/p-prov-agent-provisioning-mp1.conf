#!/usr/bin/env bash
# Merge Point Configuration: p-prov-agent-provisioning MP1 (Phase A Complete)
# Auto-generated by /run-plan on June 24, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP1"
WORKSTREAM="p-prov-agent-provisioning"
WORKSTREAM_DIR="docs/workstreams/p-prov-agent-provisioning"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P-PROV Phase A: Security fix + SA identity pool"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deeptrail-control && python -m pytest tests/api/v1/test_agent_config_auth.py -v"
    "cd deeptrail-control && python -m pytest tests/api/v1/test_agent_slots.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD="docker compose exec deeptrail-control alembic upgrade head"
BUILD_CMD="docker compose build deeptrail-control"
RESTART_CMD="docker compose up -d db deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=(
    "docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT column_name FROM information_schema.columns WHERE table_name='agents' AND column_name IN ('created_by', 'owner_user_id');\""
)

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
    "GET|/api/v1/admin/agent-slots|.total|Slots API"
    "GET|/api/v1/admin/health/agents|.total|Health agents API"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deeptrail-control/app/middleware/admin_auth.py"
    "test -f deeptrail-control/alembic/versions/*_add_agent_owner.py 2>/dev/null || test -f deeptrail-control/alembic/versions/*agent_owner*.py"
    "test -f infra/terraform/agent_slots.tf"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="Phase A\\|security fix\\|p-prov"

# Alembic migration revision (empty if not known yet)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command
DB_CHECK_CMD="docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT column_name FROM information_schema.columns WHERE table_name='agents' AND column_name='created_by';\""

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/api/v1/agents/test/config | grep -q 401"
    "cd infra/terraform && terraform validate"
)
