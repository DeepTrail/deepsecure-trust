#!/usr/bin/env bash
# Merge Point Configuration: p5.2-gap-closure MP3 (Ops Hardening Complete)
# Auto-generated by /run-plan on 2026-06-06

MP_ID="MP3"
WORKSTREAM="p5.2-gap-closure"
WORKSTREAM_DIR="docs/workstreams/p5.2-gap-closure"

WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P2-B1: Ops hardening complete — registry mode + health poller + gateway heartbeat"
MERGE_MSG=""
SYNC_PREFIX=""

UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/services/test_health_poller.py -v"
    "cd deeptrail-gateway && pytest tests/backends/test_dynamic_registry.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

MIGRATION_CMD="docker compose exec deeptrail-control alembic upgrade head"
BUILD_CMD="docker compose build deeptrail-control deeptrail-gateway frontend"
RESTART_CMD="docker compose up -d db redis deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

SMOKE_ENDPOINTS=(
    "GET|/health|.status|Control health"
)

SMOKE_TESTS=(
    "test -f deeptrail-control/app/services/health_poller.py"
    "grep -q GATEWAY_REGISTRY_MODE deeptrail-gateway/app/core/config.py"
)

COMMIT_PATTERN="health.poller\|registry.mode\|gateway.heartbeat\|p5.2-gap-closure"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

SUCCESS_CRITERIA=(
    "grep -q gateway_heartbeat deeptrail-control/app/api/v1/endpoints/internal.py || grep -q gateway/heartbeat deeptrail-control/app/api/v1/endpoints/internal.py"
)
