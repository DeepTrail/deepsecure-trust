#!/usr/bin/env bash
# Merge Point Configuration: p-prov-agent-provisioning MP2 (Phase B Complete)
# Auto-generated by /run-plan on June 24, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP2"
WORKSTREAM="p-prov-agent-provisioning"
WORKSTREAM_DIR="docs/workstreams/p-prov-agent-provisioning"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P-PROV Phase B: Composite provisioning + prompt RBAC"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deeptrail-control && python -m pytest tests/api/v1/test_provision.py -v"
    "cd deeptrail-control && python -m pytest tests/api/v1/test_prompts.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control"
RESTART_CMD="docker compose up -d deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
    "GET|/api/v1/admin/agent-slots|.total|Slots API still works"
    "GET|/api/v1/admin/health/agents|.total|Health agents still works"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deeptrail-control/app/services/provision_service.py"
    "test -f deeptrail-control/app/services/prompt_validation.py"
    "test -f deeptrail-control/tests/api/v1/test_provision.py"
    "test -f deeptrail-control/tests/api/v1/test_prompts.py"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="Phase B\\|provisioning\\|prompt RBAC\\|p-prov"

# Alembic migration revision (empty — no new migration in Phase B)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command (empty — no schema changes in Phase B)
DB_CHECK_CMD=""

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "cd deeptrail-control && python -m pytest tests/api/v1/test_provision.py tests/api/v1/test_prompts.py -q --tb=short"
)
