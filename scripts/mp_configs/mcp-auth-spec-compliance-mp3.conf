#!/usr/bin/env bash
# Merge Point Configuration: mcp-auth-spec-compliance MP3 (Security Hardening Complete)
# Auto-generated by /run-plan on 2026-05-29
# Used by: scripts/execute_merge_point.sh

MP_ID="MP3"
WORKSTREAM="mcp-auth-spec-compliance"
WORKSTREAM_DIR="docs/workstreams/mcp-auth-spec-compliance"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="MP3: Security Hardening complete — RS256, refresh tokens, revocation"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/core/test_jwt_signing.py tests/api/test_well_known_jwks.py tests/api/v1/test_agent_refresh_revoke.py -v"
    "cd deeptrail-gateway && pytest tests/middleware/test_https_enforcement.py tests/middleware/test_jwt_validation.py tests/middleware/test_audit_auth_events.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control deeptrail-gateway"
RESTART_CMD="docker compose up -d deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=(
    "docker compose up -d redis"
)

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Control Plane health"
    "GET|/.well-known/jwks.json|.keys[0].kty|JWKS endpoint"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deeptrail-control/app/api/well_known.py"
    "test -f deeptrail-control/app/core/jwt_signing.py"
    "test -f deeptrail-control/app/api/v1/endpoints/agent_auth.py"
    "test -f deeptrail-gateway/tests/middleware/test_https_enforcement.py"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="security\|rs256\|mcp-auth-spec-compliance"

# Alembic migration revision (empty if no migration)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command (empty if not needed)
DB_CHECK_CMD=""

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "cd deeptrail-control && pytest tests/core/test_jwt_signing.py tests/api/test_well_known_jwks.py tests/api/v1/test_agent_refresh_revoke.py -v --tb=short"
    "cd deeptrail-gateway && pytest tests/middleware/test_https_enforcement.py tests/middleware/test_jwt_validation.py tests/middleware/test_audit_auth_events.py -v --tb=short"
    "curl -sf http://localhost:8000/.well-known/jwks.json | jq '.keys | length'"
)
