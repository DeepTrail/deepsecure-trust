#!/usr/bin/env bash
# Merge Point Configuration: it-admin-service-catalog-mcp-mgmt MP1 (Foundation Complete)
# Auto-generated by /run-plan on May 29, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP1"
WORKSTREAM="it-admin-service-catalog-mcp-mgmt"
WORKSTREAM_DIR="docs/workstreams/it-admin-service-catalog-mcp-mgmt"

# Git worktree / branch configuration
# Empty values = single-branch mode (no worktree sync)
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P0-B1: Foundation complete — 3 tables, admin middleware, KMS wrapper, seed script"
MERGE_MSG=""

# Directory prefix to sync from main repo to worktree (empty for single-branch)
SYNC_PREFIX=""

# Test commands (run from repo root, before merge)
UNIT_TESTS=(
    "cd deeptrail-control && python -c 'from app.models.service_registry import ServiceRegistry, ServiceOAuthConfig; print(\"OK\")'"
    "cd deeptrail-control && python -c 'from app.models.delegation_template import DelegationTemplate; print(\"OK\")'"
    "cd deeptrail-control && pytest tests/core/test_kms.py -v"
    "cd deeptrail-control && pytest tests/middleware/test_admin_auth.py -v"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD="docker compose exec deeptrail-control alembic upgrade head"
BUILD_CMD="docker compose build deeptrail-control"
RESTART_CMD="docker compose up -d deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

# Additional deploy steps
DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

# Config-driven API endpoint smoke tests
# Format per entry: "METHOD|PATH|JQ_CHECK|LABEL"
SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
)

# Non-API smoke tests (file existence, build checks)
SMOKE_TESTS=(
    "test -f deeptrail-control/app/models/service_registry.py"
    "test -f deeptrail-control/app/models/delegation_template.py"
    "test -f deeptrail-control/app/core/kms.py"
    "test -f deeptrail-control/app/middleware/admin_auth.py"
    "test -f deeptrail-control/scripts/seed_admin.py"
)

# Commit message pattern for merge verification
COMMIT_PATTERN="foundation\|service_registry\|admin_auth\|kms"

# Alembic migration revision (empty if unknown yet)
EXPECTED_ALEMBIC_REV=""

# DB-specific check command
DB_CHECK_CMD="docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT count(*) FROM information_schema.tables WHERE table_name IN ('service_registry','service_oauth_configs','delegation_templates')\" -t"

# Custom success criteria commands
SUCCESS_CRITERIA=(
    "docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT column_name FROM information_schema.columns WHERE table_name='user_sessions' AND column_name='role'\" -t | grep -q role"
)
