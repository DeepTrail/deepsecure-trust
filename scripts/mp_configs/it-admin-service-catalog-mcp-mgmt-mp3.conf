#!/usr/bin/env bash
# Merge Point Configuration: it-admin-service-catalog-mcp-mgmt MP3 (Full Feature Functional)
# Auto-generated by /run-plan on May 29, 2026
# Used by: scripts/execute_merge_point.sh

MP_ID="MP3"
WORKSTREAM="it-admin-service-catalog-mcp-mgmt"
WORKSTREAM_DIR="docs/workstreams/it-admin-service-catalog-mcp-mgmt"

# Git worktree / branch configuration
WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P0-B3: Gateway dynamic loading + Admin frontend complete"
MERGE_MSG=""
SYNC_PREFIX=""

# Test commands
UNIT_TESTS=(
    "cd deeptrail-gateway && pytest tests/backends/test_dynamic_registry.py -v"
    "cd frontend && npx tsc --noEmit"
    "cd frontend && npm run build"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

# Container deployment
MIGRATION_CMD=""
BUILD_CMD="docker compose build deeptrail-control deeptrail-gateway"
RESTART_CMD="docker compose up -d deeptrail-control deeptrail-gateway"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

# Container smoke test configuration
API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

SMOKE_ENDPOINTS=(
    "GET|/health|.status|Control health"
)

# Non-API smoke tests
SMOKE_TESTS=(
    "test -f deeptrail-gateway/app/backends/dynamic_registry.py"
    "test -f frontend/src/hooks/useAdminRole.ts"
    "test -f frontend/src/lib/types/admin.ts"
    "test -d frontend/src/app/\\(dashboard\\)/dashboard/admin/services"
    "test -d frontend/src/app/\\(dashboard\\)/dashboard/admin/agents"
    "test -d frontend/src/app/\\(dashboard\\)/dashboard/admin/delegations"
    "test -d frontend/src/app/\\(dashboard\\)/dashboard/admin/health"
    "test -d frontend/.next"
)

COMMIT_PATTERN="dynamic\|frontend\|admin\|sidebar"
EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD=""

SUCCESS_CRITERIA=(
    "cd deeptrail-gateway && python -c 'from app.backends.dynamic_registry import DynamicBackendLoader; print(\"OK\")'"
)
