#!/usr/bin/env bash
# Merge Point Configuration: p5.2-gap-closure MP2 (Delegation UX Complete)
# Auto-generated by /run-plan on 2026-06-06

MP_ID="MP2"
WORKSTREAM="p5.2-gap-closure"
WORKSTREAM_DIR="docs/workstreams/p5.2-gap-closure"

WORKTREE_PATH=""
WORKTREE_BRANCH=""
TARGET_BRANCH=""
COMMIT_MSG="P1-B2: Delegation UX complete — PATCH narrow + auto-provision + invites"
MERGE_MSG=""
SYNC_PREFIX=""

UNIT_TESTS=(
    "cd deeptrail-control && pytest tests/api/test_delegation_patch.py -v"
    "cd deeptrail-control && pytest tests/api/test_delegation_accept.py -v"
    "cd deeptrail-control && pytest tests/services/test_delegation_service.py -v -k patch"
)
INTEGRATION_TESTS=()
REGRESSION_TESTS=()
VERIFY_INTEGRATION="python scripts/verify_integration.py --warn-only"

MIGRATION_CMD="docker compose exec deeptrail-control alembic upgrade head"
BUILD_CMD="docker compose build deeptrail-control frontend"
RESTART_CMD="docker compose up -d db redis deeptrail-control"
HEALTH_URL="http://localhost:8000/health"
HEALTH_TIMEOUT=60

DEPLOY_STEPS=()

API_BASE="http://localhost:8000"
LOGIN_EMAIL="test@example.com"
LOGIN_PASSWORD="testpass"

SMOKE_ENDPOINTS=(
    "GET|/health|.status|Health check"
)

SMOKE_TESTS=(
    "test -f deeptrail-control/app/services/auto_provision_service.py"
)

COMMIT_PATTERN="delegation\|PATCH\|auto.provision\|p5.2-gap-closure"

EXPECTED_ALEMBIC_REV=""
DB_CHECK_CMD="docker compose exec db psql -U deepsecure_user -d deeptrail_controldb -c \"SELECT column_name FROM information_schema.columns WHERE table_name='delegation_templates' AND column_name='auto_provision'\" -t"

SUCCESS_CRITERIA=(
    "grep -q patch_delegation deeptrail-control/app/services/delegation_service.py"
)
